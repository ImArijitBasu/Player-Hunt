import React from 'react';

import Selected from '../Selected/Selected';
const Remaining = () => {
    return (
        <div>
            <Selected></Selected>
        </div>
    );
};

export default Remaining;